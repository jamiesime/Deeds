<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.codeclan.com.deeds">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".menu_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_create" />
        <activity android:name=".activity_list_deeds" />
        <activity android:name=".activity_date" />
        <activity android:name=".activity_edit" />
        <activity android:name=".activity_filter" />
        <activity android:name=".activity_info" />
        <activity android:name=".activity_set_date"></activity>
    </application>

</manifest>